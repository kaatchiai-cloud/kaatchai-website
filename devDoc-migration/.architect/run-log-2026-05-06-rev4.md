# Architect run log — revision 4 fan-out

**Date:** 2026-05-06
**Mode:** revision-4 fan-out (skip ingest, skip partition, scope-clarification only)
**Predecessor agent:** a90cfe13ec8f9ea99 (gate-approved with 3 default decisions)
**Constraint:** no new phases, no new ADRs, no commits, plan stays 22–31 weeks
**Phase count:** 8 (unchanged); ADR count: 8 (unchanged)

---

## Approved decisions carried in from gate

| # | Decision | Resolution |
|---|----------|------------|
| 1 | P1.3 — P05 Animated boundary | **Option (a):** P05 owns the full Animated AutoPilot path including `js/17e-canvas-launch.js`. Avoids splitting Animated across P05/P06. |
| 2 | `api/kling.js` CORS lock placement | **P02:** added alongside the new `/v1/*` CORS work. Patches both `Allow-Origin: '*'` lines at `api/kling.js:8` AND `:39`. |
| 3 | P03 grep coverage | **Yes:** add `index.html` to `indexedDB.open` grep list. All 3 `stori_db` reel-handoff sites (`js/20-reels-creator.js:5799, 5814` + `index.html:4787`) treated as ONE work item replaced by `/v1/projects/import-reel/:id`. |

---

## Pre-flight verification (5 prior fixes)

| # | Fix | Status |
|---|-----|--------|
| 1 | P06 lipsync table at `migration-phase-06-...md:122` reads "Cloud Run + MediaPipe-Node" | INTACT |
| 2 | P07 BYOK exit criteria covers all 6 providers (Gemini, Kling, ElevenLabs, OpenAI, Anthropic, fal.ai) at `migration-phase-07-...md:11–21, 25–28, 53–54` + `migration-plan.md:43` | INTACT |
| 3 | mode-lock language narrowed to "video_mode immutability" at `migration-plan.md:42` | INTACT |
| 4 | Phase 04 Module Split exists (`migration-phase-04-module-split.md`) | INTACT |
| 5 | P03 has 10 tables + 7-day spike (`migration-plan.md:39, 88–105`) | INTACT |

All 5 prior fixes verified before any rev-4 edit landed.

---

## Load-bearing line ranges verified at fan-out time

| Anchor | Verified at | Result |
|--------|-------------|--------|
| `js/15-project.js` auth stub | grep | **1362–1404** — line 1363 has `setItem('stori_user', ...)`, block runs through 1404. Plan row 02 was `1363–1404`; corrected to `1362–1404`. ADR-05 already correct. P02 already correct. |
| `api/kling.js` CORS | grep | Two `Allow-Origin: '*'` lines at **`:8`** (preflight) and **`:39`** (response) |
| `js/21-kling.js` Kling JWT | grep | `generateKlingJWT()` at `:12`; `stori_kling_access_key` at `:13`; `stori_kling_secret_key` at `:14`; call sites at `:37` (submit) and `:70` (poll) |
| `callGeminiAPI` invocations | grep | **50 hits across 9 files** (`js/17a-create-api.js` is definition + ~16 self-calls; `17b`, `17c`, `20`, `24`, `26`, `26b`, `31`, `32` are call sites) |
| `trackCost()` call sites | grep | 3 call sites in `js/17a-create-api.js` at `:472, :730, :785` |
| `js/17a-create-api.js` key-getters | grep | `getCreateGeminiKey()` at `:288`, `getFreeKey()` at `:291`, `getPaidKey()` at `:292`; sibling getters (`getPPApiKey`, `getReelApiKey`, `getReelFreeKey`, `getReelPaidKey`) live in same module |
| `index.html` loader contract | Read | Eager `<script>` block at lines ~4715–4719; dynamic loader array at `4731–4744`; `28-canvas-consistency.js` is currently eager at `:4718` |
| `build.js` MAIN_FILES claim | grep | **No `MAIN_FILES` symbol exists.** `build.js` auto-discovers via static-script regex at `:69–78` and dynamic-loader scan at `:82–90`. P04 phrasing corrected. |
| `index.html` `stori_db` site | grep | One IDB site at `:4787` (inside Marketing-Pipeline reel-handoff block at `:4782–4800`) |
| `js/20-reels-creator.js` `stori_db` sites | grep | Two IDB sites at `:5799, :5814` |
| Local-only UI prefs | grep | `01-core.js:543` theme; `17c-create-pipeline.js:1090, 1099, 1104` autosave; `17c-create-pipeline.js:5171, 5180` agent panel collapsed; `26-brainstorm.js` per-session brainstorm cache |
| Audio coupling | grep | `js/32-audio-input.js:1127–1128` calls `window.persistPerSceneAudio`, defined at `js/33-audio-rehearsal.js:109` |

---

## Files modified by rev-4 fan-out (no commits — left in working tree)

### Plan + index
- `/Users/praveen/Desktop/stori/migration-plan.md` — P3.1 line range fix (1363→1362); revision note bumped to rev-4 with delta summary; P05 row entry/exit updated (option (a) + Kling-key grep); P07 row exit updated (callGeminiAPI/key-getter/trackCost grep); P3.2 canvas line-count corrections (4,498 → 840 in two places); 13,507 → 9,849; P04 row "MAIN_FILES" → loader contract.

### Phase docs
- `/Users/praveen/Desktop/stori/migration-phase-02-auth-migration.md` — added P1.1 callGeminiAPI shared-abstraction note to in-scope item 3; new in-scope items 3a (eager-load preamble) and 3b (api/kling.js CORS lock); expanded out-of-scope to itemize 6 providers; expanded Files-touched with `api/kling.js`; new work-breakdown sections §5.0 (eager-load) + §5.0a (kling CORS).
- `/Users/praveen/Desktop/stori/migration-phase-03-api-contract-and-project-state.md` — P2.1 storage-scope rewording with explicit local-only carve-outs (theme/autosave/agent-panel/brainstorm cache); BYOK secrets noted as P07 territory; Decision 3 unifies 3 `stori_db` sites as one work item; grep gate updated to cover `index.html`; P2.5 vercel.json rewrites task added (in-scope item 11 + work-breakdown §5.5a); Modified-files list expanded.
- `/Users/praveen/Desktop/stori/migration-phase-04-module-split.md` — P2.2 build/load contract correction throughout: removed every `build.js MAIN_FILES` reference, replaced with explicit `index.html` loader edits (dynamic-loader array at 4731–4744 + eager `<script>` block at ~4717–4718); §5.4 retitled and rewritten; technology table updated; Files-touched updated to mark `build.js` as NOT modified; Links section updated.
- `/Users/praveen/Desktop/stori/migration-phase-05-autopilot-pipeline-extraction.md` — P1.3 option (a) added as in-scope items 8a (Animated boundary, 17e allowed) + 8b (server-side Kling JWT migration in 21-kling.js) + 8c (callGeminiAPI replacement strategy for AutoPilot files); §C.5 work breakdown rewritten with explicit Kling-JWT delete + 17e cutover + scoped callGeminiAPI sweep; Forbidden-files list updated (17e moved to Modified); Modified list updated; exit criteria 10 + 11 added (Kling-key grep + 17e cutover grep).
- `/Users/praveen/Desktop/stori/migration-phase-06-secondary-pipelines-extraction.md` — P1.4 §B.2.0 MediaPipe Node-port spike (1–2 days, blocking) inserted with 4 spike checks + fallback contract; spike artefact path `devDoc-migration/06-mediapipe-spike.md` declared; Open Questions row 1 reworded to reflect spike gate; Risks table updated with 2 new rev-4 rows; P2.4 ElevenLabs 4 endpoints enumerated in audio in-scope item 6 (`/v1/audio/transcribe`, `/v1/audio/voices`, `/v1/audio/tts`, `/v1/audio/tts-with-timestamps`); audio-input + audio-rehearsal coupling note added (P3.6); exit criterion 5a added (`api.elevenlabs.io` grep); P1.1 callGeminiAPI strategy for secondary files added to in-scope item 8; goal line corrected (~13,507 → ~9,849, with the 29-canvas-render carve-out explained).
- `/Users/praveen/Desktop/stori/migration-phase-07-web-cutover.md` — P3.5 deletion sweep expanded: in-scope item 2 enumerates `callGeminiAPI` def + 7 key-getters (`getCreateGeminiKey`, `getPPApiKey`, `getReelApiKey`, `getFreeKey`, `getPaidKey`, `getReelFreeKey`, `getReelPaidKey`) + `trackCost` (3 call sites at 472/730/785); exit criterion 3 grep widened; §5.2 BYOK deletion sweep expanded + §5.2 confirms P05 already deleted Kling JWT (catch-net here).

### Inventory + sidecars
- `/Users/praveen/Desktop/stori/devDoc-migration/.architect/spec-inventory.md` — P3.4 O2 marked **[DEFERRED — future mobile cycle; this revision-4 plan ships web only.]** in both override-text and resulting-constraint columns.
- `/Users/praveen/Desktop/stori/migration-details.md` — P3.3 SUPERSEDED banner inserted directly under H1 (one-line block; no other content edited).

### Verified-only (no changes)
- `/Users/praveen/Desktop/stori/migration-adr-05-auth-and-session.md` — already uses `1362–1404`; no edit needed.
- `/Users/praveen/Desktop/stori/devDoc-migration/spec-coverage-matrix.md` — grep confirmed no stale `4,498` / `13,507` references; no edit needed.

### Files NOT touched (per hard constraints)
- All other ADRs (01, 02, 03, 04, 06, 07, 08).
- Phase 01 doc and Phase 08 doc — not in rev-4 scope.
- `/Users/praveen/Desktop/stori/app/*` — separate workstream.
- `/Users/praveen/Desktop/stori/pricing-plan.md` — never read.
- All source code (`js/*.js`, `index.html`, `api/*.js`, `build.js`, `vercel.json`) — phase docs reference these; rev-4 does not modify code.

---

## Git posture

Working tree left dirty. **No `git add`, no `git commit`, no `git stash`.** User stages and commits at their own discretion. Suggested staging order on the user's side:

1. Plan + inventory (`migration-plan.md`, `devDoc-migration/.architect/spec-inventory.md`)
2. Banner (`migration-details.md`)
3. Phase docs in dependency order (02 → 03 → 04 → 05 → 06 → 07)
4. Run log (`devDoc-migration/.architect/run-log-2026-05-06-rev4.md` — this file)

---

## Verification status

All P1.1–P1.5, P2.1–P2.5, P3.1–P3.6 deltas applied. The 5 prior fixes remain intact (re-verified post-edit by inspection of the same anchors).

*End of rev-4 architect run log.*
