# Spec inventory — Stori canvas-graph redesign

## Part 1 — Metadata

```
Spec form:           multi-source (HTML mock + accompanying narrative + 4 code files)
Primary spec:        /Users/praveen/Desktop/stori/canvas-redesign-mock.html
Mock line count:     865
Mock word count:     3,205
Mock top-level sections: 0 markdown headings (HTML/CSS comment banners used as sections)
Code-context files:
  - /Users/praveen/Desktop/stori/js/29-canvas-render.js   (2,388 lines)
  - /Users/praveen/Desktop/stori/js/27-canvas-state.js    (560 lines)
  - /Users/praveen/Desktop/stori/js/17c-create-pipeline.js (3,303 lines)
  - /Users/praveen/Desktop/stori/css/canvas-graph.css     (1,362 lines)
Adjacent context:
  - /Users/praveen/Desktop/stori/index.html               (DOM structure + Aurora dark tokens at L17–60)
  - /Users/praveen/Desktop/stori/css/styles.css           (Family A; #create-page Aurora remap at L404–418; specificity hazard at L3677)
  - /Users/praveen/Desktop/stori/css/themes.css           (Aurora light overrides — full --lp-* and Family A redefinitions)
  - /Users/praveen/Desktop/stori/css/themes-inventory.md  (canonical Aurora theme system source-of-truth)
  - /Users/praveen/Desktop/stori/build.js                 (inline pipeline)
Version / date:      not stated (mock is undated)
Ingest timestamp:    2026-05-01T00:00:00 (working date)
Revised:             2026-05-01T00:30:00 — Aurora-first scope change folded in (revision 1)
```

The "spec" is a hybrid: an HTML mock that is authoritative for visuals + a long-form natural-language brief in the architect prompt + the existing Aurora theme system that the redesign must derive from rather than replace. Section IDs below refer to CSS comment banners in the mock (`/* === BANNER === */`) and to the architect-supplied design narrative segments.

**Aurora-first amendment (revision 1):** The mock's hard-coded color values are NO LONGER authoritative for the production redesign. The mock is authoritative for layout, structure, sockets-on-edges pattern, curve styling, and interaction patterns. **All chrome / background / border / text / accent colors come from the existing Aurora `--lp-*` token system** (defined for `aurora.dark` in `index.html:17–60` and for `aurora.light` in `css/themes.css:27–81`). New `--cg-*` / `--sock-*` tokens are only added where Aurora doesn't already cover the role. Both `aurora.dark` and `aurora.light` are first-class — every visual decision must work in both. See Part 4 #1 (revised) for the namespace decision and Part 4 #14 (new) for the light-mode parity guarantee.

## Part 2 — Section map

| # | Heading | Line range | Category | Key claims (≤3) | Constraints | Open questions |
|---|---------|------------|----------|-----------------|-------------|----------------|
| 1 | Mock — THEME TOKENS (light & dark) | mock 7–49 | technology | Two `[data-theme]` blocks with `--bg-*`, `--text*`, `--accent`, `--grid-dot`, `--sock-*` palette; sock palette is yellow/orange/purple/cyan/blue per type. **Mock's hex values are illustrative only — production uses Aurora `--lp-*` for chrome/bg/border/text/accent. Only socket palette and dot-grid are net-new.** | Sock palette must exist for both themes; Aurora `--lp-*` is the source of truth for chrome | None — resolved to Aurora-first (Part 4 #1, revised) |
| 2 | Mock — STORI TOP HEADER | mock 61–78 | workflow | 56px fixed top bar; brand + crumb + theme-toggle button; unchanged in redesign | height = 56px; z-index 1000 | None |
| 3 | Mock — LEFT ICON RAIL | mock 80–110 | workflow | 56px wide vertical rail with abstract icons + tooltips; "active" rail icon highlighted | width = 56px; positioned `top: 56px; left: 0; bottom: 0` | **CONFLICT: architect prompt says KEEP & restyle existing `#create-agent-panel` (~220–240px, named agent steps), NOT this generic icon rail. Resolve in partition.** |
| 4 | Mock — CANVAS AREA (.canvas-stage) | mock 112–123 | architecture | Stage occupies `top:56px; left:56px; right:0; bottom:0`; dot-grid via `radial-gradient(circle at 1px 1px, var(--grid-dot) 1px, transparent 0) 0 0 / 24px 24px`; cursor `grab` | Grid spacing 24px; dot 1px | Architect prompt requests left = 220–240px (or 56px collapsed), not 56px fixed. Background color comes from `--lp-bg` (Aurora), not the mock's `--bg-base`. |
| 5 | Mock — TOP ACTION PILL | mock 125–171 | workflow | Floating pill at top-center; contains Project / Star / Undo / Redo / Share / drag-handle / batch stepper / Run primary / Run dropdown ▾ / Cancel ✕ / "1 active" status / right-pane toggle | `position: absolute; top:16px`; `border-radius: 999px`; pulsing status dot animation 1.2s; pill background derived from `--lp-card` + blur (Aurora glass aesthetic) | Wire targets (Run, Cancel, batch counter) named in prompt but exact pipeline functions unverified |
| 6 | Mock — PROGRESS STRIP | mock 173–190 | workflow | Top-right slim strip showing `Total: x%` + 100px mini bar + `Node: y%` | `top:70px; right:18px`; bar width 100px height 3px; bar fill = `--lp-accent` (cyan in dark, deeper cyan in light) | Source events for "Total" vs "Node" not specified |
| 7 | Mock — BOTTOM-LEFT TELEMETRY | mock 192–201 | non-functional | Mono-font block: T (time), I (images), N (nodes), V (CANVAS_LAYOUT_VERSION), FPS | Font: SF Mono / Menlo / Consolas; 11px; pointer-events none; text color = `--lp-faint` | FPS measurement method (rAF probe) not implemented yet |
| 8 | Mock — BOTTOM-RIGHT ZOOM DOCK | mock 203–220 | workflow | Floating pill bottom-right: cursor mode dropdown / Fit / Zoom % / Minimap / Reset | `border-radius: 999px`; right:14px; bottom:14px; pill bg = `--lp-card` translucent | Minimap and Connect cursor mode are no-ops in v1 |
| 9 | Mock — NODE GRAPH LAYER | mock 221–232 | architecture | `.graph-layer` is the zoom-transformed inner layer; `transform-origin: 0 0`; SVG curves overlay with `pointer-events: none` | Curves SVG sized 100% × 100%; `overflow: visible` | None |
| 10 | Mock — NODES (base + selected + head) | mock 234–276 | workflow | `.node` base style: `bg-elevated`, `border-strong`, `border-radius: 10px`; `.selected` adds 1px accent ring; `.node-head` is a single line with type-color dot + title + (optional) tabs | All nodes share base style; type-colored head-dot. Production: card bg = derived from `--lp-card` / `--bg-elevated`; border = `--lp-card-bdr`; selected ring = `--accent` (Aurora cyan). | None |
| 11 | Mock — VARIANT TRAY | mock 278–293 | workflow | Dashed rectangle wrapping ACTIVE card + thumb strip; tray label top-left negative-margin chip | `border: 1px dashed var(--border-strong)`; `pointer-events: none`; `z-index: 0`; production border = `--lp-card-bdr-h` dashed | Auto-sizing of tray rectangle to wrap dynamic content not specified |
| 12 | Mock — GHOST/ADD CARD | mock 294–304 | workflow | Dashed-border ghost card placeholder for "add new variation" | `:hover` flips border to `--accent` (Aurora cyan in production) | None |
| 13 | Mock — VARIANT-PIN (ACTIVE pill) | mock 306–313 | workflow | Top-right corner pill on ACTIVE card; reads "ACTIVE" | `font-family: SF Mono`; uses `color-mix(in oklch, var(--accent) 12%, transparent)` (Aurora cyan tint in production) | `color-mix` browser-support floor: oklch supported in modern browsers, no fallback needed for project's target audience |
| 14 | Mock — THUMBNAIL STRIP | mock 315–366 | workflow | 56×36 wrap-grid thumbs with index number; `.has-vids` adds purple-tinted border (`--sock-video` mix); `.vid-badge` adds `▶N` count chip; `.thumb.add` is dashed `+` tile | Thumb dims 56×36; gap 6px; padding 10px×12px. `.has-vids` border in BOTH themes uses `color-mix(in oklch, var(--sock-video) 60%, var(--border))`. | "+" tile click handler binding (existing addVariation pipeline) unverified |
| 15 | Mock — SOCKETS (.sock) | mock 368–380 | architecture | 12px circular bumps on node edges; `.in` left:-8px, `.out` right:-8px; `top:16px`; type colors via `--sock-{script\|image\|video\|audio\|final}` | All nodes share socket dimensions; SVG strokes read socket color via `getComputedStyle`. **Socket palette is the only net-new color set in this redesign — must be defined for BOTH `aurora.dark` and `aurora.light`.** | **Audio socket vs Aurora accent collision (Part 4 #1 revised): mock's `--sock-audio: #4ee0c8` (teal-cyan) collides with Aurora `--lp-accent` (cyan). Resolution in ADR-1.** |
| 16 | Mock — SB BODY (textarea) | mock 382–388 | workflow | `.sb-script` is a transparent borderless textarea, min-height 88px, font 12px; text color inherits from `--text` → `--lp-text` | `resize: none`; inherits font | Existing `#create-page textarea` rule overrides this; specificity bump required (already in canvas-graph.css L959) |
| 17 | Mock — IMG / VID PREVIEW | mock 390–398 | workflow | `.img-preview` 16:9 aspect; `background-size: cover`; gradient placeholder for empty state (light/dark variant of placeholder gradient required) | aspect-ratio 16/9 | Empty-state gradient must be defined for both themes; Aurora dark uses `--lp-bg2` tinted; light uses `--lp-card` tinted |
| 18 | Mock — INLINE STEPPERS | mock 400–411 | workflow | `.stepper` row with `◀ value ▶` arrows; flex-equal sibling steppers in `.inline-row`; chrome derives from `--lp-card` / `--lp-card-bdr` | font-size 11px; padding 4×8 | Click handlers on `.arr` not implemented in mock; binding to instance fields TBD |
| 19 | Mock — STATUS DOT | mock 413–420 | workflow | Top-right corner dot; `.done` cyan, `.running` yellow + pulse, `.pending` faint, `.error` red. **Done = `--sock-audio` (teal/mint, post-collision-resolution); running = `--sock-script` (yellow); pending = `--text-faint`; error = a canvas-scoped `--cg-danger` (Aurora doesn't cover this role; falls back to existing `--red` / `--lp-red`).** | 6px dia | Error state not styled in mock (red dot implied but absent); production resolves error color via Aurora's `--red` (Family A) or a new `--cg-danger`; both themes verified |
| 20 | Mock — SELECTION TOOLBAR | mock 422–436 | workflow | Floating pill above selected card; buttons: Regen / Variation / Download / Delete; Delete uses `.danger` color (Aurora `--red`) | Sample positions card-relative `top:-72px; left:50%`; pill bg = `--lp-card` glass | Pipeline action handlers named but not verified to exist |
| 21 | Mock — DOM: top header | mock 442–448 | reference | Stori brand + crumb + theme-toggle button | — | None |
| 22 | Mock — DOM: left rail | mock 450–464 | reference | 12 generic icon entries | — | **Conflict with prompt: must be replaced by restyled #create-agent-panel** |
| 23 | Mock — DOM: top pill | mock 469–487 | reference | Buttons rendered as in spec | — | None |
| 24 | Mock — DOM: progress strip | mock 489–494 | reference | Static placeholder values 14% / 60% | — | Live wiring TBD |
| 25 | Mock — DOM: graph layer + curves | mock 496–610 | reference | SB1 + IMG1 tray + IMG1 ACTIVE + IMG1 thumb-strip + VID1 tray + VID1 ACTIVE + VID1 thumb-strip + BGM + Final | Hard-coded positions in px | Layout currently hand-placed; production will use `runLayout()` from 29-canvas-render.js |
| 26 | Mock — DOM: telemetry block | mock 612–619 | reference | 5 stat lines (T/I/N/V/FPS) | — | None |
| 27 | Mock — DOM: zoom dock | mock 621–628 | reference | 5 pills | — | None |
| 28 | Mock — JS: toggleTheme + drawCurves | mock 633–671 | architecture | Curves drawn between active-path pairs; stroke pulled via `getComputedStyle().getPropertyValue('--sock-' + type)`; `1.6` solid for active vs `1.2` dashed; bezier control offset `max(80, dx*0.5)` | Stroke widths 1.2/1.6; opacity 0.45/0.9. **`getComputedStyle` returns the value resolved against the active `[data-theme]`; this gives free theme support for curves IFF `--sock-*` is defined in both themes.** | Hover-dim non-connected to 25% (Phase 12) is a delta over mock |
| 29 | Mock — JS: ZOOM + PAN | mock 673–721 | workflow | Wheel zoom centered at pointer; clamp 0.25–2.5; drag-to-pan when target is not chrome/node | clamp [0.25, 2.5]; factor `exp(-deltaY * 0.0015)` | E key for fit-to-view: in prompt only, not in mock |
| 30 | Mock — JS: DATA MODEL (SB1) | mock 723–771 | architecture | Mock model: `SB1.activeTab`, `SB1.tabs[X].activeImg`, `SB1.tabs[X].images[]`, `SB1.tabs[X].vids[imgIdx] = { active, list }` | Active idx is a number ("activeImg"), not a string ID | **Mismatch with real schema (see Part 4 #2): real model uses isActive/isRenderActive flags on instances, not numeric `activeXxxIdx` fields** |
| 31 | Mock — JS: rerender + tab handlers | mock 773–862 | workflow | Single `rerender()` re-runs all DOM updates; tab click sets `SB1.activeTab` + rerender; thumb click sets `tab.activeImg` + rerender; resize redraws curves | rerender is full-tree; no diffing | Real implementation uses node `update*Node` patterns + `redrawCurves` |
| 32 | Architect — Visual direction | prompt prose | vision | "ComfyUI-grade" premium feel; theme-aware dot grid; edge sockets on every node; color-typed connections; floating chrome | — | "Premium/professional/functional" is qualitative |
| 33 | Architect — Data hierarchy & active path | prompt prose | architecture | scene → storyboardInstances[A] → imageInstances[k] → videoInstances[v]; active path emits edges; switching active morphs descendants | Active at every level is single | **Hierarchy stated as nested videoInstances under imageInstances; ACTUAL CODE puts videoInstances at the scene level keyed by sourceImageInstanceId. Decision in ADR.** |
| 34 | Architect — Element-by-element map | prompt prose | reference | 17 entities listed: header, agent panel, top pill, progress strip, telemetry, zoom dock, canvas bg, properties pane, SB node, IMG tray, IMG ACTIVE, IMG strip, VID tray, VID ACTIVE, VID strip, BGM, Final | — | Properties pane scope minimal in this redesign |
| 35 | Architect — Interactions | prompt prose | workflow | Wheel zoom, pan, E-key fit, Tidy, click thumb, click tab, right-click context, theme toggle | Zoom clamp 0.25–2.5 | Marquee/Delete-key/double-click socket are deferred to QoL phase |
| 36 | Architect — Phase plan (0–13) | prompt prose | roadmap | 14 named phases (Phase 0 theme tokens through Phase 13 QoL) | **14 phases exceeds architect's hard cap of 9** | Must consolidate in partition step |
| 37 | Architect — ADR list (1–11) | prompt prose | risk | 11 cross-cutting decisions enumerated | All 11 are real cross-cutting; none confined to single phase | None — direct ADR inputs |
| 38 | Architect — Quality bar | prompt prose | non-functional | No orphans (every entity → UI + workflow + data binding); every ADR has context/decision/rationale/alternatives/consequences; "verify before implementing" notes required where code behavior uncertain | — | None |
| 39 | Code — 27-canvas-state.js header doc | js27 1–26 | architecture | Real schema: `scene.storyboardInstances[].imageInstances[]` (nested) + `scene.videoInstances[]` (FLAT, NOT nested under imageInstance); each video has `sourceImageInstanceId` | Active-flag invariants: `storyboard.isActive` RADIO; `imageInstance.isActive` MULTI-SELECT; `imageInstance.isRenderActive` RADIO; `videoInstance.isRenderActive` RADIO | Mock's "activeImg" / "activeVid" numeric idx fields do not match this model — ADR-2 must reconcile |
| 40 | Code — 27 CRUD API | js27 222–346 | architecture | `setActiveStoryboard`, `toggleImageVideoGenActive`, `setImageRenderActive`, `setVideoRenderActive`, `addStoryboardInstance`, `deleteStoryboardInstance`, `addImageInstance`, `deleteImageInstance`, `addVideoInstance`, `deleteVideoInstance` | All exposed via `window.CanvasState` (L536) | None — this is the canonical instance API |
| 41 | Code — 29-canvas-render public API | js29 various | architecture | `window.CanvasGraph = { mount, unmount, refresh, notifyImageReady, fitToView, tidyLayout, getScenes, isActive }`; `CANVAS_LAYOUT_VERSION = 6`; column constants `COL_LAUNCH/COL_BGM_*/COL_FINAL_*`; `runLayout()` resets stale `canvasPosition` when version changes | LAYOUT_VERSION constant at L20 | When to bump and migration semantics — ADR-3 |
| 42 | Code — 29 internal nodes | js29 523–1450 | architecture | Per-type build/update functions: `buildSBNode/updateSBNode`, `buildImgNode/updateImgNode`, `buildVidNode/updateVidNode`, `renderLaunchNode`, `renderBgmNode`, `renderSubtitleNode`, `renderFinalNode`; curve drawing in `redrawCurves` (L1450) → `drawCurve` (L1562) → `bezier` (L1575) | One render module owns DOM | Subtitle node not present in mock; this redesign keeps it (out-of-scope confirmation needed) |
| 43 | Code — 29 zoom/pan/fit | js29 1668–1742 | workflow | `applyTransform`, `handleZoom(delta, cx, cy)`, `fitToView()`; uses internal view object (verify field names against zoom-dock wiring) | — | Verify whether existing zoom code clamps 0.25–2.5 like mock does |
| 44 | Code — 29 action handlers | js29 1942–2020 | workflow | `doAddImageInstance`, `doDeleteSB`, `doDeleteImage`, `doDeleteVideo`, `doDownloadImage`, `doPreviewImage`; these are the EXISTING canonical handlers | All are file-internal (closure-scoped); not on window | **Architect's ADR-6 names `regenImage/addVariation/deleteImage/downloadImage/addVideoVariation` — only `doDownloadImage` and `doDelete*` exist by similar name; `regenImage` and `addVideoVariation` are NOT defined. ADR-6 must reconcile naming + add missing.** |
| 45 | Code — 17c-create-pipeline.js public API | js17c various | architecture | `launchImageAgent` (L3216), `openCanvasPanel` (L3243), `closeCanvasPanel` (L3277), `updateSceneCardImage` (L2731), `updateSceneCardStatus` (L2753), `updateStepStates` (L807); also `window.regenerateScene` (L2814) | `launchImageAgent` is the entry point that flips `body.canvas-active` and mounts CanvasGraph | Full call graph from "Run" button on top pill to image generation pipeline not yet traced |
| 46 | Code — canvas-graph.css specificity guard | cg-css 957–974 | technology | `.cg-prompt` was overridden by global `#create-page textarea`; bumped to `#create-canvas-step textarea.cg-prompt` to win | Selector depth: `#id elem.class` | Any new canvas CSS that targets an element type already styled at `#create-page elem` level needs the same bump — ADR-11 |
| 47 | Code — canvas-graph.css zoom-invariant pattern | cg-css 1135–1148 | technology | Pattern: column applies `transform: scale(calc(1 / var(--cg-zoom, 1)))` so chrome inside graph layer counter-scales; `.cg-prompt` uses `font-size: calc(8px + 8px / var(--cg-zoom, 1))` for partial inverse-scaling | `--cg-zoom` set by JS on transform | Where else this should apply: selection toolbar, steppers — ADR-10 |
| 48 | Code — index.html DOM contract | index.html 1974–2482 | architecture | `#create-agent-panel` (L1974) is sibling of `#create-workflow` (L1998); `#create-canvas-panel` (L2476) is the parallel sibling shown when `body.canvas-active`; `#create-canvas-step` (L2482) is the canvas mount target | Panel structure must be preserved; canvas mounts INSIDE `#create-page` and inherits the `#create-page` Aurora token remap (styles.css:404–418) | None |
| 49 | Code — build.js inline pipeline | build.js 14–26, 120 | technology | `styles.css` and `themes.css` are inlined into `dist/index.html`; `canvas-graph.css` is NOT inlined (remains a separate `<link>` with `?v=` cache-bust) | Inline order: styles → themes → existing inline | Decision: inline canvas-graph.css too, or keep separate. ADR-8 |
| 50 | Code — styles.css `#create-page textarea` rule | styles.css 3677–3690 | technology | This rule is the source of the historical override that broke `.cg-prompt` | Selector `#create-page textarea` | Future canvas widgets that use textarea/input/etc inside `#create-page` will need explicit specificity bumps |
| 51 (new) | Aurora theme system — `--lp-*` tokens | index.html:17–60 (dark) + css/themes.css:27–81 (light) | architecture | Aurora is the brand token system that drives Landing, Copilot (`#create-page`), and Autopilot. **Dark accent: `--lp-accent: oklch(80% 0.14 200)` (cyan); light accent: `--lp-accent: oklch(50% 0.16 220)` (deeper cyan).** Aurora provides `--lp-bg / --lp-bg2 / --lp-card / --lp-card-bdr / --lp-card-bdr-h / --lp-text / --lp-dim / --lp-faint / --lp-accent / --lp-accent2 / --lp-glow` in BOTH themes. Family A on `#create-page` is remapped to Aurora at styles.css:404–418 (`--accent: var(--lp-accent)`, `--bg-primary: var(--lp-bg)`, etc.) | Canvas mounts inside `#create-page` and INHERITS the Aurora remap automatically. | **Audio socket vs Aurora accent collision (mock's `--sock-audio: #4ee0c8` cyan-teal vs Aurora cyan): must be resolved in ADR-1. Recommended resolution: shift socket-audio to a teal/mint hue (`#1ea895` from mock light or similar), reserving cyan for the brand accent only.** |
| 52 (new) | Aurora theme inventory — themes-inventory.md | css/themes-inventory.md (full file) | reference | Source-of-truth doc for the Aurora theme system. Section "Family B — Aurora palette (`--lp-*`)" enumerates every Aurora token + light-mode counterpart. Section "Hardcoded colors that are INTENTIONALLY not themed" lists media-content exceptions (video letterbox, modal backdrops). | Aurora is dark-first historically but `aurora.light` is fully defined and shipped (see `css/themes.css`). | None — this doc is the canonical reference; phase docs cite it. |
| 53 (new) | Recent commit context — Aurora cyan theme | git log abc3e49 ("Aurora cyan theme: replace purple accents") | reference | Aurora was migrated from purple (`#7c3aed`) to cyan (`oklch(80% 0.14 200)` dark / `oklch(50% 0.16 220)` light) accent system. **The mock's blue accent (`#4a9eff`) maps to Aurora cyan in production.** | All `--accent` references inside `#create-page` resolve to Aurora cyan. | None — settled. |

## Part 3 — Glossary

| Term | Definition (as spec / code states it) | Line ref |
|------|---------------------------------------|----------|
| ACTIVE | "this branch's chosen variant" — persisted to scene state via `isActive` (radio for SB) or `isRenderActive` (radio for IMG/VID) flags. Shown as ACTIVE pill on the card. | architect prompt; js27:23–26 |
| SELECTED | "the user clicked this" — UI ephemeral, not persisted; shown as 1px accent ring | architect prompt |
| SB | Storyboard — scene-level script container; one or more storyboard instances ("variants") per scene; tabs A/B/+ in the redesign | mock:502; js27:10 |
| IMG | Image instance — generated image variant under a storyboard; one is `isRenderActive` per scene | mock:529; js27:12 |
| VID | Video instance — generated video clip linked to a source image via `sourceImageInstanceId` | mock:561; js27:17 |
| BGM | Background Music — single per-scene audio node; routes Lyria / Library / Skip | mock:584; architect prompt |
| FINAL | Final render node — single per-project node with resolution/fps stepper and Render button | mock:597 |
| Sock / Socket | Edge-attachment bump on a node (`.sock.in` / `.sock.out`); colored by data-type; SVG strokes read color via getComputedStyle | mock:368–380; architect prompt |
| Tray / Variant tray | Dashed rectangle wrapping ACTIVE card + thumbnail strip for one node-type at one branch level | mock:278; architect prompt |
| Thumb / Thumbnail strip | Wrap-grid of small (~56×36) variant thumbnails below the ACTIVE card | mock:315; architect prompt |
| has-vids | Thumbnail-level CSS class indicating that the IMG variant has ≥ 1 child video | mock:355–361 |
| vid-badge | "▶N" count chip on a thumbnail bottom-right | mock:343–353 |
| Active path | Single chain SB → IMG → VID → BGM → Final selected by the active flags at each level; only this path emits edges | architect prompt |
| Chrome | The floating UI elements outside the graph layer (top pill, progress strip, telemetry, zoom dock, agent panel) — `position: fixed`, NOT zoom-transformed | architect prompt |
| Graph layer | The zoom-transformed inner container (`#graph` in mock) holding nodes + SVG curves | mock:222 |
| Top pill | Floating top-center action bar (Project/Star/Undo/Redo/Share/batch/Run/Cancel/status/right-pane toggle) | mock:128 |
| Zoom dock | Floating bottom-right pill (cursor mode / Fit / Zoom% / Minimap / Reset) | mock:206 |
| Telemetry | Bottom-left mono-font readout (T / I / N / V / FPS) | mock:195 |
| Progress strip | Top-right slim bar (Total% + bar + Node%) | mock:176 |
| CanvasState | The pure-state module exposed at `window.CanvasState`; no DOM, owns instance CRUD + migration + validation | js27:5; js27:536 |
| CanvasGraph | The render module exposed at `window.CanvasGraph`; mount/unmount/refresh/notifyImageReady/fitToView/tidyLayout/getScenes/isActive | js29 (architect prompt) |
| CANVAS_LAYOUT_VERSION | Module-level constant (currently 6); bumping invalidates saved `canvasPosition` so `runLayout()` re-places nodes | js29:20 |
| canvasPosition | Per-instance `{x,y}` position persisted on storyboard / image / video instances | js27:73,85,103 |
| isActive | Per-instance flag — RADIO for storyboard (one per scene); MULTI-SELECT for imageInstance ("⭐ Use for video gen") | js27:23–24 |
| isRenderActive | Per-instance flag — RADIO for imageInstance (illustrated mode chosen render) and for videoInstance (animated mode chosen render) | js27:25–26 |
| Properties pane | Existing right-side detail pane that shows fields for the selected node | architect prompt |
| ComfyUI | Reference visual style — node-graph editor with edge sockets and color-typed wires; "ComfyUI-grade" used as quality target | architect prompt |
| `--cg-*` | Canvas-scoped CSS custom-property namespace. **Post-Aurora-revision: this namespace exists ONLY for canvas-specific roles not covered by Aurora (e.g. `--cg-grid-dot`, `--cg-pill-blur`, `--cg-danger` if not derivable from Aurora).** | cg-css passim |
| `--sock-*` | Socket-type CSS custom-property namespace (script/image/video/audio/final). **Net-new in this redesign; defined in BOTH `aurora.dark` and `aurora.light`.** | mock:24–28; cg-css |
| `--cg-zoom` | JS-set CSS variable used by `calc()` rules to counter-scale text/widgets at low zoom | cg-css:967, 1148 |
| `body.canvas-active` | Body-level class flipped on when the canvas panel is mounted; toggles which panel is visible | js17c (search); cg-css:1265 |
| ADR | Architecture Decision Record — Nygard format; one decision per file | architect convention |
| **Aurora (theme system)** | The brand token system (`--lp-*`) that drives Landing, Copilot (`#create-page`), and Autopilot. Defined in `index.html:17–60` for `aurora.dark` and `css/themes.css:27–81` for `aurora.light`. Token-swap architecture: `[data-theme="dark"]` and `[data-theme="light"]` rebind the same variable names. **Canvas redesign reuses Aurora; does NOT create a parallel palette.** | index.html:17–60; themes.css:27–81; themes-inventory.md |
| **`--lp-*`** | Aurora token namespace. Provides `--lp-bg / --lp-bg2 / --lp-card / --lp-card-bdr / --lp-card-bdr-h / --lp-text / --lp-dim / --lp-faint / --lp-accent / --lp-accent2 / --lp-glow / --lp-bdr / --lp-bdr-h` in both themes. | index.html:42; themes.css:30 |
| **`aurora.dark` / `aurora.light`** | Two first-class theme variants. **Both ship together; no "dark first, light later".** | themes.css; index.html |
| **Aurora cyan accent** | `oklch(80% 0.14 200)` in dark; `oklch(50% 0.16 220)` in light. The brand accent. **Mock's `#4a9eff` blue accent maps to Aurora cyan in production (post-commit abc3e49).** | index.html:42; themes.css:39 |
| T3SP | undefined — not used in this spec | n/a |

## Part 4 — Cross-cutting decision signals

| # | Decision topic | Sections referenced | Stated position | Spec / code lines |
|---|----------------|---------------------|-----------------|-------------------|
| 1 (revised) | **Theme token namespace — Aurora-first.** Reuse Aurora `--lp-*` for chrome / bg / border / text / accent (covered by existing token-swap architecture). Add `--cg-*` ONLY for canvas-specific roles Aurora doesn't cover (`--cg-grid-dot`, `--cg-pill-blur`, `--cg-danger` if not derivable). Add `--sock-*` for the socket-type palette (5 type colors), defined in BOTH `aurora.dark` and `aurora.light`. **Resolve audio-vs-accent collision:** mock's `--sock-audio: #4ee0c8` (cyan-teal) collides with Aurora's cyan accent; resolution = remap socket-audio to teal/mint (`#1ea895` from mock light, or equivalent oklch). **Folds in conflict #12 (left-panel identity rule).** Token placement: `--sock-*` in `themes.css` (so theme toggle covers them); `--cg-*` may live in `canvas-graph.css` if scoped to `#create-canvas-step`. | mock §1; cg-css passim; architect ADR-1; themes-inventory.md §1; styles.css:404–418 | Aurora-first wins. Mock's hex values are illustrative only. SVG strokes read computed-style at curve draw time, so `--sock-*` defined per `[data-theme]` gives free theme support. | mock:24–28; mock:651–652; cg-css throughout; index.html:17–60; themes.css:27–81 |
| 2 | Active-path single source of truth — mock model (`activeStoryboardIdx/Img/Vid` numeric idx) vs real model (`isActive` / `isRenderActive` boolean flags on instances). Migration of existing scenes that already use the flag model. | architect ADR-2; js27:22–26; mock:726–771 | **Real model wins** (already in production); redesign uses `isActive`/`isRenderActive`; mock's idx-based model is illustrative only. Backwards compat = no change. **Folds in conflict #13.** | js27:22–26, 222–245; mock:730–752 |
| 3 | `CANVAS_LAYOUT_VERSION` bump strategy — when to bump, how `runLayout` resets stale `canvasPosition`, mid-redesign user data implications | architect ADR-3; js29:20; js29:265 | Bump on any saved-position-invalidating layout change; runLayout already null-clears stale positions | js29:20; js29:runLayout L265 |
| 4 | Active vs Selected separation — two distinct state concepts; rules for interaction (selecting a thumbnail also makes it active; selecting empty stage clears selection but not active) | architect ADR-4; mock §10, §13, §20 | ACTIVE = persisted (instance flag); SELECTED = ephemeral UI; both can apply at once | mock:307 (.variant-pin); mock:247 (.node.selected) |
| 5 | Variant tray rendering strategy — DOM nodes for now; threshold (~50+ thumbs per strip) at which to reconsider canvas/virtualization | architect ADR-5; mock §11, §14 | DOM for MVP; revisit at 50+ thumbs | n/a (architect-stated) |
| 6 | Action pipeline integration — context menu and selection toolbar both call canonical handlers (regenImage, addVariation, deleteImage, downloadImage, addVideoVariation). One handler per action. | architect ADR-6; js29:1942–2020; js17c:2814, 3216 | Verify and consolidate. **Findings: `doDownloadImage` exists; `doDelete*` exists; `doAddImageInstance` exists; `regenImage`-named handler does NOT exist (only `regenerateScene`); `addVideoVariation` does NOT exist (state-level `addVideoInstance` exists in CanvasState, no pipeline glue).** New canonical names + thin wrappers required. | js29:1942 (do*); js17c:2814 (regenerateScene); js27:315 (addVideoInstance) |
| 7 | Backwards compat for saved projects — old scenes have `imgDataUrl` / `status` / `videoUrl` / `videoClips` on the scene directly; `migrateAllScenes` mirrors these into instances. Mirror direction (legacy → instances, never reverse), and removal timeline. | architect ADR-7; js27:42–108, 123–164 | Migration is idempotent; legacy-flat → instance mirror is one-way; `syncMirrorFields` (L123) writes legacy fields back FROM instances for older code paths that still read them — confirm direction in ADR | js27:42–108; js27:123–164 |
| 8 | Build pipeline for canvas-graph.css — currently NOT inlined into dist; `<link rel="stylesheet" href="css/canvas-graph.css?v=…">` is loaded separately. Decision: keep separate (must bump `?v=` on every change) or inline via build.js. Recommend inlining for cache-safety. | architect ADR-8; build.js:14–26, 120 | Recommend inlining; preserve load order (styles → themes → canvas-graph) | build.js:14–26 |
| 9 | Zoom-invariant chrome vs zoom-transformed graph — chrome (left rail/agent panel, top pill, progress strip, telemetry, zoom dock) is `position: fixed` outside graph layer; node graph + curves inside `#graph` which gets the zoom transform. Selection toolbar lives inside graph layer (transforms with node). | architect ADR-9; mock:128/177/195/207/224/422 | Confirmed by mock; document the rule so future chrome additions don't accidentally end up zoomed | mock:128, 224, 422 |
| 10 | Font sizing at low zoom — current `.cg-prompt` rule uses `calc(8px + 8px / var(--cg-zoom, 1))` partial inverse-scaling. Document pattern; selection toolbar buttons + steppers must stay readable at low zoom. | architect ADR-10; cg-css:967, 1135–1148 | Establish a reusable mixin/utility for zoom-counter-scaling text inside graph layer | cg-css:967, 1148 |
| 11 | Specificity guard — `#create-page textarea` rule overrode `.cg-prompt`; resolved by bumping selector to `#create-canvas-step textarea.cg-prompt`. Document hazard so new canvas CSS doesn't get silently overridden by global `#create-page` rules. | architect ADR-11; cg-css:957–974; styles.css:3677 | Codify rule: any element type styled by a `#create-page X` rule that also appears inside `#create-canvas-step` must get the `#create-canvas-step` prefix | styles.css:3677; cg-css:957 |
| 12 (folded) | Left side panel identity — generic 56px icon rail (mock §3, §22) vs restyled 220–240px `#create-agent-panel` with named agent steps (architect prompt). | mock §3 vs architect prompt | **Architect prompt wins**; mock's icon rail is decorative-only; production must keep agent panel with click-to-jump-to-column behavior. **Folded into ADR-1.** | mock:80–110; index.html:1974 |
| 13 (folded) | Schema mismatch — architect prompt says `imageInstances[].videoInstances[]` (nested); real code stores `videoInstances[]` flat at scene level keyed by `sourceImageInstanceId` (js27:17, js27:315). | architect prompt §"Data hierarchy"; js27:17 | **Real schema wins**; UI groups videos under their source image at render time via the `sourceImageInstanceId` join. **Folded into ADR-2.** | js27:17, 92, 315 |
| **14 (new) — Light-mode parity guarantee.** | Both `aurora.light` AND `aurora.dark` are first-class theme variants; every visual decision (sockets, curves, backgrounds, chrome translucency, focus rings, hover states, status dots, count badges, selection outlines, dot-grid alpha) must work and be tested in BOTH. **Hazard pattern: developer designs in dark, light variant gets contrast/saturation issues** (e.g. cyan that looks great on `--lp-bg: #050814` washes out on `--lp-bg: #eef2f7`). Mock light-mode values (mock:30–49) are a starting reference; production verifies via Aurora `--lp-*` resolution. | architect prompt (revision 1); themes.css; themes-inventory.md §6 (gotchas), §7 (verification) | All phase acceptance criteria touching color/contrast/theming must include "verify in `aurora.light` AND `aurora.dark`" steps. Phases affected: P01 (token defs), P02 (chrome translucency + accent buttons + progress fill), P03 (panel translucency + status dots), P04 (sockets + curves + selection ring + status dot + hover-dim), P05 (tray dashed border + thumbnail .has-vids border + vid-badge + ACTIVE pill), P06 (VID tray + thumbnail play-icon mark), P07 (selection toolbar + context menu + Delete .danger), P08 (marquee + Properties pane restyle). | n/a — new architect requirement (revision 1) |

> Note: items #12 and #13 are folded into ADRs 1 and 2 respectively. **#14 (new) opens ADR-12 — light-mode parity guarantee.** Total ADR count: 12 (1–11 architect-enumerated + 12 new from revision 1).

## Part 5 — Unanchored claims

| # | Source | Claim | Why unanchored |
|---|--------|-------|----------------|
| 1 | architect prompt | "ComfyUI-grade node graph" | Qualitative target; no measurable acceptance criterion (e.g. "all 5 socket colors visible at 100% zoom in both themes") |
| 2 | architect prompt | "Premium, professional, functional" | No definition; partition must turn this into per-phase acceptance bullets |
| 3 | architect prompt §interactions | "Wheel zoom centered at pointer (clamp 0.25–2.5)" | Mock matches (mock:695); existing 29-canvas-render `handleZoom` clamp values not verified |
| 4 | architect prompt §interactions | "E key (or Fit button) → fit to view with safe-area inset for chrome" | "Safe-area inset" magnitude unspecified; existing `fitToView` (js29:1694) inset value not verified |
| 5 | architect prompt | "agent step rows with status dot matching socket type colors" | Mapping from agent type (Storyboard/Script/Image/BGM/Animation/Render) to sock palette (script/image/video/audio/final) is implied but not enumerated. **Audio dot uses post-collision-resolution teal/mint (Part 4 #1 revised), not Aurora cyan.** |
| 6 | architect prompt | "click step row → scroll/zoom canvas to that column band" | "Column band" is the per-step column constant in 29-canvas-render (`COL_LAUNCH`/`COL_BGM_*`/etc); mapping needs to be made explicit per agent type |
| 7 | architect prompt | "batch stepper" + "1 active" status pill values | Wired to "running queue" — what queue? Existing pipeline has `launchImageAgent`; no batch counter today. Phase 1 must define the source of truth. |
| 8 | architect prompt | "Total: x% Node: y%" progress | Source events not specified. Code spans `updateSceneCardStatus`, `notifyImageReady`, plus subtitle/BGM/render polls. Mapping needed per phase. |
| 9 | architect prompt | "FPS = rAF probe" | No tolerance / threshold; debug-only display is fine, but Phase 3 acceptance must spell out display behavior when FPS drops |
| 10 | architect prompt | "minimap" | Listed without spec. Phase 2 should explicitly mark it as no-op stub. |
| 11 | architect prompt | "Cursor mode dropdown (Select / Pan / Connect — Connect can be no-op)" | Connect mode is a future hook; v1 acceptance = Connect button exists, click is a no-op (or visibly disabled). |
| 12 | architect prompt | "selection toolbar floats 72px above selected card" | Mock confirms `top:-72px` (mock:542); but at zoom < 1 the absolute distance shrinks. Toolbar zoom-counter-scaling rule to be in ADR-10. |
| 13 | architect prompt | "purple-tinted border on thumbs that have videos" | Mock implements via `color-mix(in oklch, var(--sock-video) 60%, var(--border))` (mock:357). `oklch` is supported in Aurora's target browsers (themes.css already uses oklch). **Light-mode variant: mix percentage may need to shift since `--border` differs (rgba on dark vs rgba on light); verify in both themes per ADR-12.** |
| 14 | architect prompt | "+ tile generates new video from active IMG" (VID strip add) | Pipeline glue does not exist; must be added in Phase 6 (was Phase 9 in architect plan). State-level `CanvasState.addVideoInstance` exists (js27:315) but no pipeline call site yet. |
| 15 | architect prompt | Phase-13 "double-click socket auto-creates next-stage node" | "Next-stage node" semantics are nontrivial — adds a new instance at the next pipeline column, with what defaults? Phase 8 acceptance must spell out. |
| 16 | architect prompt | "Right Properties pane: Existing; restyled to match (translucent, dense rows, no big card chrome)" | Properties pane scope is mostly out-of-scope for this redesign per phase plan (no Phase named for it); decide explicitly whether it gets its own phase or rolls into the "card chrome shrink" phase. **Restyle uses Aurora `--lp-card` translucency in both themes per ADR-12.** |
| 17 (new) | revision 1 | "use Aurora theme; both dark and light" | Resolved via Part 4 #1 (revised) and #14 (new) → ADR-1 + ADR-12. |
| 18 (new) | revision 1 | "audio socket vs Aurora accent collision" | Resolved via Part 4 #1 (revised): remap socket-audio to teal/mint, reserve cyan for brand accent. Resolution documented in ADR-1. |

## Part 6 — Ingest pattern log

- **Read coverage:** mock 1–865 (all in two chunks); js27 1–110 + targeted greps for CRUD and active-flag fields (covers schema + CRUD section); js29 targeted greps for top-level functions, version constant, video references; js17c targeted greps for the named pipeline functions; canvas-graph.css targeted greps for specificity bump + zoom-invariant rule; styles.css confirmation of `#create-page textarea` rule + Aurora remap at L404–418; build.js confirmation of inline pipeline; index.html confirmation of `#create-agent-panel` / `#create-canvas-panel` / `#create-canvas-step` IDs and Aurora dark token block at L17–60; themes.css full read; themes-inventory.md full read.
- **Skipped:** full sequential read of js29 (2,388 lines) and js17c (3,303 lines) — they are reference implementations, not the spec. Phase docs must read them targeted to the phase scope.
- **Categories — populated:** vision, architecture, technology, workflow, non-functional, roadmap, risk, reference (all populated). No empty primary category.
- **Cross-cutting decision signals (Part 4):** 11 architect-enumerated + 2 conflict-derived (Part 4 #12 schema-mismatch, #13 panel-identity, both folded) + 1 new from revision 1 (#14 light-mode parity). **ADR count: 12** (1–11 + new 12).
- **Conflicts surfaced for partition:**
  1. Mock uses generic 56px icon rail; architect prompt requires 220–240px restyled `#create-agent-panel`. Architect prompt wins (folded into ADR-1).
  2. Mock data model uses `activeXxxIdx` numeric fields; real code uses `isActive` / `isRenderActive` boolean flags. Real code wins (folded into ADR-2).
  3. Architect prompt describes data hierarchy as `imageInstances[].videoInstances[]` (nested); real code stores `videoInstances` flat at scene level. Real code wins (folded into ADR-2).
  4. Architect's ADR-6 names handlers (`regenImage`, `addVariation`, `addVideoVariation`) that do not exist by those names in current code. Phase 7 must either rename or add canonical wrappers — ADR-6 decides.
  5. **(revision 1)** Mock encodes its own `--bg-base / --accent / --sock-*` palette; architect (revision 1) requires Aurora-first. Aurora wins for chrome; `--sock-*` is the only net-new palette. Audio socket vs Aurora cyan collision resolved by remapping audio to teal/mint. Folded into ADR-1.
  6. **(revision 1)** "Light mode" cannot be deferred. Both themes ship together; hazard pattern documented as ADR-12.
- **Phase-count ceiling:** architect-supplied plan lists 14 phases (0–13); architect harness cap is 9. Partition consolidated to 8 phases. **Aurora revision does NOT add a new phase** — Aurora work folds into existing P01 + ADR-1 + new ADR-12; affected acceptance criteria added to P01, P02, P03, P04, P05, P06, P07, P08.
- **Formatting anomalies:** mock is HTML/CSS/JS — not markdown — so heading discovery is by CSS comment banner pattern (`/* === BANNER === */`) and DOM comment pattern (`<!-- ============== … ============== -->`). Section line ranges in Part 2 are accurate but section "headings" are CSS comments.
- **No invented line numbers.** Every Part 2 / Part 4 / Part 5 row was either Read directly or grep-verified above.
- **Architect-supplied prompt is included as authoritative spec text** (entries #32–38, plus revision-1 amendment entries #51–53). Treating it as a "section" of the spec because it carries load-bearing decisions (phase plan, ADR list, quality bar, Aurora-first directive) that the mock does not encode.

## Part 7 — Revision log

### Revision 1 — 2026-05-01T00:30:00 — Aurora-first scope change

**Trigger:** user directive — "we should use aurora theme. have it in plan. both dark and light."

**Changes folded in (no new phases added):**

1. **Section #1 (Mock — THEME TOKENS)** annotated: mock hex values illustrative only; production uses Aurora `--lp-*` for chrome.
2. **Sections #4–8, #10–14, #16–20** annotated to cite Aurora token sources (e.g. "pill bg = `--lp-card` glass") instead of mock's hex codes.
3. **Section #15 (sockets)** flagged audio-vs-accent collision; resolution noted (ADR-1).
4. **Section #19 (status dot)** updated mapping to post-collision-resolution colors.
5. **New sections #51 (Aurora theme system), #52 (themes-inventory.md), #53 (commit abc3e49 cyan-not-purple)** added to Part 2.
6. **Glossary** gained five entries: Aurora (theme system), `--lp-*`, `aurora.dark` / `aurora.light`, Aurora cyan accent. Updated `--cg-*` and `--sock-*` definitions to reflect Aurora-first scoping.
7. **Part 4 #1 (revised)** Aurora-first decision; subsumes #12 (left-panel identity).
8. **Part 4 #14 (new)** light-mode parity guarantee; opens ADR-12.
9. **Part 5 #13** annotated for light-mode variant of `.has-vids` mix.
10. **Part 5 #16** Properties pane restyle uses Aurora translucency in both themes.
11. **Part 5 #17 (new)** "use Aurora; both dark and light" → resolved.
12. **Part 5 #18 (new)** audio-vs-accent collision → resolved.

**No section removed.** All prior content preserved; revisions are additive annotations or replacement in-place where the prior text was strictly wrong (e.g. "production palette = mock palette" → "production palette = Aurora palette").

**Phase-count change:** none. Still 8 phases, cap 9.
**ADR-count change:** 11 → 12 (added ADR-12 for light-mode parity).
**Coverage matrix change:** add new rows for sections #51 / #52 / #53 (all map to P01 + multiple downstream phases); add ADR-12 row.
