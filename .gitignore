.DS_Store

# Pre-ship audit working docs — timestamped plan files, kept locally only
pre-ship-audit-*.md

# Design mocks, prototypes, and engineering docs — local study/reference only
design/
devDoc/
canvas-preview.html
canvas-timeline-send.html

# Tool artifacts
.playwright-mcp/

# Flutter app build artifacts
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/.pub-cache/
app/.pub/
app/pubspec.lock

# Secrets — NEVER commit credentials
migration credential.rtf
.env
.env.*
!.env.example

# Node / infra
node_modules/
dist/
build/
.pnpm-store/

# Infra secrets
infra/**/.env
infra/**/.env.*
